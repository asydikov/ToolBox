apiVersion: v1
kind: Service
metadata:
  name: {{ template "toolboxservicessqlmonitor.fullname" . }}
  labels:
    app: {{ template "toolboxservicessqlmonitor.name" . }}
    chart: {{ template "toolboxservicessqlmonitor.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "toolboxservicessqlmonitor.name" . }}
    release: {{ .Release.Name }}
